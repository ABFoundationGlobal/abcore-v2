# See http://help.github.com/ignore-files/ for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile ~/.gitignore_global

*/**/*un~
*/**/*.test
*un~
.DS_Store
*/**/.DS_Store

#*
.#*
*#
*~
.project
.settings

# used by the Makefile
/build/_workspace/
/build/cache/
/build/bin/
/geth*.zip

# used by the build/ci.go archive + upload tool
/geth*.tar.gz
/geth*.tar.gz.sig
/geth*.tar.gz.asc
/geth*.zip.sig
/geth*.zip.asc


# travis
profile.tmp
profile.cov

# IdeaIDE
.idea
*.iml

# VS Code
.vscode

cmd/geth/node/
cmd/geth/__debug_bin
cmd/bootnode/bootnode
graphql/__debug_bin

tests/spec-tests/

# binaries
/geth
cmd/abidump/abidump
cmd/abigen/abigen
cmd/blsync/blsync
cmd/clef/clef
cmd/devp2p/devp2p
cmd/era/era
cmd/ethkey/ethkey
cmd/evm/evm
cmd/geth/geth
cmd/rlpdump/rlpdump
cmd/workload/workload
cmd/keeper/keeper

# ABCore project files
.claude/
CLAUDE.md

# Compat Clique suite generated data
script/compat-clique-v1-v2/data/
script/compat-clique-v1-v2/genesis.json
script/compat-clique-v1-v2/bin/

# Transition test Python venv (created by 94-run-tx-test.sh)
script/transition-test/.venv

# Release script runtime data (chain state, keystore, configs staged for docker)
script/release/data/

# parliagenesis top-level validators.conf — working copy staged by the Makefile
# (`cp $(OUTDIR)/validators.conf ...`) during `make build`/`make devnet`. The
# per-environment default/ and devnet/ validators.conf files ARE tracked; this
# top-level one is a transient build artifact.
core/systemcontracts/parliagenesis/validators.conf

# Local devnet keystores (contain private keys — never commit)
script/devnet/

# Transition test Python venv (current path; created by the transition drill)
script/test/transition/.venv/

# Upgrade-drill runtime chain data
script/test/upgrade-drill/data/
